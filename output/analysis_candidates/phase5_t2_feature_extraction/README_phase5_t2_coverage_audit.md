# Phase 5 T2 Coverage Audit

T2 patients: `61`. Active tables: `16`. Active selected features: `67`.

The light table is excluded from this active audit because repeated database disconnects made its current output unreliable. Features with at least 10% patient coverage are placed in the working exploratory set; features below 10% are retained as sensitivity-only. This threshold is a POC screening rule, not a clinical validity threshold.
